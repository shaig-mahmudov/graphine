[package]
name = "benchmark-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Graphine Phase 0 benchmark corpus validator and reporter"

[dependencies]
anyhow = "1.0"
jsonschema = { version = "0.33", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

[lints]
workspace = true

