# java-core fixture

Pure Java 17 fixture for symbol discovery, overloads, interfaces, inheritance, overrides, generics, records, annotations, nested classes, lambdas, method references, constructor calls, and field access. `src/analysis-only/java` intentionally references a missing external type and is excluded from Maven compilation.

Build with `mvn test` when Maven is available.
