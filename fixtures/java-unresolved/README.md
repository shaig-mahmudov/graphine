# java-unresolved fixture

Analyzer-only Maven fixture with a deliberately missing `com.vendor.remote` dependency. Safe-mode analysis must emit unresolved binding diagnostics instead of inventing resolved graph edges. This fixture is not expected to compile.
