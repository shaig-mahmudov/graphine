# spring-beans fixture

Spring Boot 3 / Java 17 bean-resolution fixture. It includes constructor injection, multiple implementations, `@Primary`, `@Qualifier`, `@Bean`, `@Profile`, a conditional bean, and one deliberately ambiguous injection point represented without starting an application context.
