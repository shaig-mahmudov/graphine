# spring-data fixture

Spring Data JPA / Java 17 fixture with one-to-many and many-to-one entities, derived queries, explicit JPQL, and transactional read/write service methods. H2 is runtime-only; no database or network is used by benchmark validation.
