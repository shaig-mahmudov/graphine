# spring-web fixture

Spring Boot 3 / Java 17 fixture covering composed routes, path variables, validated request DTOs, controller-service-repository flow, and application event publication. The repository is deliberately in-memory so this fixture stays focused on web flow.

Build with `mvn test` when dependencies are available.
